import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

interface RentalProps { }

export const RentalScreen: React.FC<RentalProps> = (props) => {

    const { } = props

    return (
        <View>
            <Text>RentalScreen</Text>
        </View>
    )
}

const styles = StyleSheet.create({})